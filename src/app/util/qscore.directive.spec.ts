import { QScoreDirective } from './qscore.directive';

describe('qScoreDirective', () => {
  it('should create an instance', () => {
    // const directive = new QScoreDirective(ElementRef);
    // expect(directive).toBeTruthy();
  });
});
